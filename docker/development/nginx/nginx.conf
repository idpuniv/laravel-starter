events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    tcp_nopush      on;
    tcp_nodelay     on;
    keepalive_timeout 65;
    client_max_body_size 50M;  # Pratique pour uploads Laravel

    server {
        listen 80;
        server_name events.pitechserver.com;

        root /var/www/public;
        index index.php index.html index.htm;

        # Route principale Laravel
        location / {
            try_files $uri $uri/ /index.php?$query_string;
            add_header Cache-Control "no-store, no-cache, must-revalidate, private" always;
        }

        # Cache pour fichiers statiques
        location ~* \.(css|js|png|jpg|jpeg|gif|svg|woff2?)$ {
            expires 1y;
            add_header Cache-Control "public, max-age=31536000, immutable";
            access_log off;
        }

        # PHP-FPM
        location ~ \.php$ {
            fastcgi_pass php-fpm:9000;  # Ton container PHP-FPM
            fastcgi_index index.php;
            include fastcgi_params;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;

            # Forward HTTPS headers (utile si derrière reverse proxy)
            fastcgi_param HTTPS on;
            fastcgi_param HTTP_X_FORWARDED_PROTO https;
            fastcgi_param HTTP_X_FORWARDED_PORT 443;
            fastcgi_param HTTP_X_FORWARDED_HOST $host;
            fastcgi_param HTTP_X_FORWARDED_FOR $proxy_add_x_forwarded_for;

            fastcgi_hide_header X-Powered-By;

            add_header Cache-Control "no-store, no-cache, must-revalidate, private" always;
        }

        # Interdire l'accès aux fichiers sensibles
        location ~ /\.(ht|env|git|svn) {
            deny all;
        }

        location ~ /\. {
            deny all;
            access_log off;
            log_not_found off;
        }

        location ~* \.(log|sqlite)$ {
            deny all;
        }

        # Silence favicon et robots.txt
        location = /favicon.ico {
            log_not_found off;
            access_log off;
        }

        location = /robots.txt {
            log_not_found off;
            access_log off;
        }

        server_tokens off;
    }
}